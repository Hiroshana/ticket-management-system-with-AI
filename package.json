{
  "name": "ticket-management-system",
  "private": true,
  "workspaces": ["client", "server"],
  "scripts": {
    "dev": "bun run --filter '*' dev",
    "dev:server": "bun run --filter server dev",
    "dev:client": "bun run --filter client dev",
    "test:e2e": "NODE_ENV=test bun run --filter server db:create:test && NODE_ENV=test bun run --filter server db:migrate:test && NODE_ENV=test bun run --filter server db:seed:test && bunx playwright test",
    "test:e2e:ui": "NODE_ENV=test bun run --filter server db:create:test && NODE_ENV=test bun run --filter server db:migrate:test && NODE_ENV=test bun run --filter server db:seed:test && bunx playwright test --ui"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2",
    "@types/node": "^25.5.0"
  }
}
